(() => {
  const storageKey = "test-risk-chats-v1";
  const $ = id => document.getElementById(id);
  const newChat = () => ({ id: crypto.randomUUID(), title: "New analysis", messages: [] });
  let chats; try { chats = JSON.parse(localStorage.getItem(storageKey) || "[]"); } catch { chats = []; }
  if (!Array.isArray(chats) || !chats.length) chats = [newChat()];
  let current = chats[0].id;
  const save = () => localStorage.setItem(storageKey, JSON.stringify(chats));
  const chat = () => chats.find(item => item.id === current) || chats[0];
  document.body.innerHTML = '<div class="risk-app"><aside><b>Robinity Intelligence</b><button id="new">＋ New analysis</button><small>ANALYSIS HISTORY</small><nav id="chats"></nav><footer>Robinity Intelligence<br><span>Local conversation history</span></footer></aside><main><header><strong>Risk Intelligence</strong><span>Contract & token analysis</span></header><section id="thread"></section><div class="compose"><div><textarea id="input" rows="1" placeholder="Paste a contract address or Pump.fun link…"></textarea><button id="send">↑</button></div><p>Analyses are informational snapshots, not investment advice.</p></div></main></div>';
  const style = document.createElement("style");
  style.textContent = 'body{margin:0;background:#101010;color:#e9e9e9;font:14px/1.5 system-ui,sans-serif}.risk-app{display:flex;min-height:100vh}.risk-app aside{width:265px;background:#171717;border-right:1px solid #292929;padding:18px 12px;display:flex;flex-direction:column}.risk-app aside b{letter-spacing:.1em;padding:8px 10px 22px}.risk-app aside i{font-style:normal;color:#a99cff}.risk-app aside>button{border:1px solid #3c3c3c;background:#222;color:#eee;border-radius:9px;text-align:left;padding:11px;cursor:pointer}.risk-app aside small{color:#888;font-size:10px;letter-spacing:.1em;margin:26px 10px 8px}.risk-app nav{display:grid;gap:3px}.chat{display:flex;background:transparent;border-radius:8px}.chat.active{background:#292929}.chat button{border:0;background:transparent;color:#ddd;padding:9px 8px;text-align:left;cursor:pointer}.chat .pick{flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.chat .delete{color:#888;font-size:18px}.risk-app aside footer{margin-top:auto;color:#999;font-size:11px;padding:12px 10px}.risk-app aside footer span{color:#666}.risk-app main{flex:1;min-width:0;background:radial-gradient(ellipse at 50% -20%,#25203b,#101010 44%)}.risk-app main>header{height:58px;padding:0 28px;border-bottom:1px solid #ffffff0d;display:flex;flex-direction:column;justify-content:center}.risk-app main>header span{font-size:11px;color:#888}.risk-app #thread{width:min(800px,calc(100% - 40px));margin:auto;padding:42px 0 150px;display:grid;gap:28px}.empty{text-align:center;padding-top:12vh;color:#999}.empty h1{color:#eee;font-size:29px;letter-spacing:-.04em}.message{display:grid;grid-template-columns:30px 1fr;gap:13px}.avatar{width:30px;height:30px;border-radius:9px;background:#303030;display:grid;place-items:center;font-size:10px;font-weight:800}.assistant .avatar{background:#8272e5;color:#171421}.bubble{padding:4px 0;white-space:pre-wrap}.card{white-space:normal;margin-top:14px;background:#181818;border:1px solid #363636;border-radius:14px;padding:16px}.scoreline{display:flex;justify-content:space-between;gap:15px;align-items:center}.scoreline b{font-size:29px;letter-spacing:-.06em}.scoreline span{padding:4px 9px;border-radius:99px;font-size:11px;font-weight:800}.low{background:#173627;color:#7ee5a5}.moderate{background:#483719;color:#f1c76d}.high,.critical{background:#461f28;color:#ff939b}.card ul{list-style:none;padding:0;margin:14px 0 0}.card li{display:flex;justify-content:space-between;border-top:1px solid #303030;padding:9px 0;color:#d0d0d0}.card li span:last-child{color:#ff9ba2}.card small{color:#888}.compose{position:fixed;bottom:0;left:265px;right:0;padding:20px max(20px,calc((100vw - 840px)/2));background:linear-gradient(transparent,#101010 25%)}.compose>div{display:flex;gap:10px;padding:10px 10px 10px 15px;background:#242424;border:1px solid #444;border-radius:15px}.compose textarea{flex:1;resize:none;border:0;outline:0;background:transparent;color:#eee;font:inherit;padding:7px 0}.compose button{height:33px;width:33px;border:0;border-radius:9px;background:#c9c1ff;color:#181522;font-size:20px;font-weight:800;cursor:pointer}.compose p{text-align:center;color:#666;font-size:11px;margin:8px}@media(max-width:720px){.risk-app aside{display:none}.compose{left:0;padding:14px}.risk-app #thread{width:calc(100% - 28px)}}';
  document.head.append(style);
  function reportCard(report) {
    const card = document.createElement("div"), line = document.createElement("div"), score = document.createElement("b"), badge = document.createElement("span"), list = document.createElement("ul"), meta = document.createElement("small");
    card.className = "card"; line.className = "scoreline"; score.textContent = report.score + "/100 safety"; badge.className = report.severity; badge.textContent = report.severity === "low" ? "HIGH SAFETY" : report.severity === "moderate" ? "MODERATE SAFETY" : report.severity === "high" ? "LOW SAFETY" : "CRITICAL RISK"; line.append(score,badge);
    (report.criteria || report.signals || []).filter(item => item.status !== "clear").slice(0,7).forEach(item => { const li=document.createElement("li"),t=document.createElement("span"),p=document.createElement("span");t.textContent=item.title;p.textContent="−"+(item.points||item.weight||0);li.append(t,p);list.append(li); });
    if (!list.children.length) { const li=document.createElement("li");li.textContent="No risk signals were returned by the connected providers.";list.append(li); }
    meta.textContent=report.name+" · "+report.symbol+" · "+report.providers.join(" + ");card.append(line,list,meta);return card;
  }
  function message(item) { const row=document.createElement("article"),avatar=document.createElement("div"),bubble=document.createElement("div");row.className="message "+item.role;avatar.className="avatar";avatar.textContent=item.role==="user"?"YOU":"AI";bubble.className="bubble";bubble.textContent=item.text;if(item.report)bubble.append(reportCard(item.report));row.append(avatar,bubble);return row; }
  function render() {
    const list=$("chats");list.replaceChildren();chats.forEach(item=>{const row=document.createElement("div"),pick=document.createElement("button"),del=document.createElement("button");row.className="chat"+(item.id===current?" active":"");pick.className="pick";pick.textContent=item.title;pick.onclick=()=>{current=item.id;render()};del.className="delete";del.textContent="×";del.onclick=()=>{chats=chats.filter(x=>x.id!==item.id);if(!chats.length)chats=[newChat()];current=chats[0].id;save();render()};row.append(pick,del);list.append(row)});
    const thread=$("thread");thread.replaceChildren();if(!chat().messages.length){const empty=document.createElement("section");empty.className="empty";empty.innerHTML="<h1>What would you like to analyse?</h1><p>Paste a token contract address or a Pump.fun link. I’ll extract the asset and review its on-chain risk signals.</p>";thread.append(empty)}else chat().messages.forEach(item=>thread.append(message(item)));
  }
  function assetFrom(text) { let match=text.match(/pump\.fun\/(?:coin\/)?([1-9A-HJ-NP-Za-km-z]{32,44})/i);if(match)return{chain:"solana",address:match[1]};match=text.match(/0x[a-fA-F0-9]{40}/);if(match)return{chain:"ethereum",address:match[0]};match=text.match(/\b[1-9A-HJ-NP-Za-km-z]{32,44}\b/);return match?{chain:"solana",address:match[0]}:null; }
  async function send() { const input=$("input"),text=input.value.trim();if(!text)return;const room=chat(),asset=assetFrom(text);room.messages.push({role:"user",text});if(room.title==="New analysis")room.title=text.slice(0,32);input.value="";if(!asset){room.messages.push({role:"assistant",text:"I couldn’t identify a supported contract address. Paste an EVM address, a Solana mint address, or a Pump.fun coin link."});save();render();return}const waiting={role:"assistant",text:"Reading on-chain risk signals…"};room.messages.push(waiting);save();render();try{const response=await fetch("/api/risk/analyze",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(asset)}),data=await response.json();if(!response.ok)throw Error(data.error);waiting.text=data.report.score>=80?"Analysis complete. Review the detailed checks below before making a decision.":"Analysis complete. Review the highlighted warnings below before interacting with this token.";waiting.report=data.report}catch(error){waiting.text="I couldn’t complete the analysis: "+error.message}save();render(); }
  $("send").onclick=send;$("input").addEventListener("keydown",event=>{if(event.key==="Enter"&&!event.shiftKey){event.preventDefault();send()}});$("new").onclick=()=>{const item=newChat();chats.unshift(item);current=item.id;save();render()};render();
})();
const editorialTheme = document.createElement("script");
editorialTheme.src = "/risk-editorial-theme.js";
document.body.append(editorialTheme);
const aiVibe = document.createElement("script");
aiVibe.src = "/risk-ai-vibe.js";
document.body.append(aiVibe);
const motionLayer = document.createElement("script");
motionLayer.src = "/risk-motion.js";
document.body.append(motionLayer);
